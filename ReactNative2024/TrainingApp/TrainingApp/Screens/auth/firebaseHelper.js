import { initializeApp } from '@react-native-firebase/app';

// Đối tượng cấu hình Firebase của bạn
const firebaseConfig = {
  // Các thuộc tính cấu hình của bạn ở đây
};

// Khởi tạo Firebase
const app = initializeApp(firebaseConfig);
